# secret-hoard
This project is used to manage static secrets for testing.  The secrets are created in a manually generated  CSV file then automatically created in secretsmanager.  Metadata is in the secret id and tags
